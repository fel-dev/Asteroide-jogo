{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run uv (WSL) - F5",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/main.py",
      "console": "integratedTerminal",
      "preLaunchTask": "Run uv (WSL)"
    }
  ]
}
